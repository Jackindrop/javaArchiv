package werkzeuge;

/**
 * This Class have only one method which called and
 * override the existing toString() method.
 */
public class SomeClass {
    /**
     * Override existing toString-method
     * @return String with the Class-identity
     */
    @Override public String toString() {
        return "This is SomeClass";
    }
}